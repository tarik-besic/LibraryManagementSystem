


    $(document).ready(function() {
      
  
      
        $("#search_users_btn").on('click',(function(){
          
        
            // let name=$("#search_users_input_name").val();
            // name=name.trim();//to remove spaces before
            // arr=name.split(" "); //spliting first and last name and then removing unnecessary spaces before and after first and last name
            // arr.forEach(element => {
            //     name.trim();
            // });
            // if(arr.length>1){  //if last name is sent by client then add it..else just use first name
            //     name=arr.shift()+" "+arr.pop();
            // }
            // else {
            //     name=arr.shift();
            // }
            // console.log(name);
        
            // $.ajax({
            //     type:"get",
            //     url:"http://localhost:5000/getusers/",
            //     contentType:"application/json",
            //     data:name
            //   });
    
        }))
            
  
        });
    